frozen-lockfile=false
prefer-frozen-lockfile=false

# Isolamento: impede que pnpm opere em projetos vizinhos do monorepo
# Usar sempre: pnpm install --filter . (ou make install)
recursive-install=false
dedupe-injected-deps=false
